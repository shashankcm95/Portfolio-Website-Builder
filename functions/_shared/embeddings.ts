/**
 * Phase 9 — Embeddings corpus stub (repo copy).
 *
 * This file is a STUB. At publish time, `src/lib/generator/chatbot-bundle
 * .ts` overwrites this file in the Pages *output* directory with a
 * baked copy that contains the portfolio's real EMBEDDINGS, OWNER_NAME,
 * etc. The repo copy stays empty so:
 *
 *   1. TypeScript compiles the Function without the publisher having
 *      run (dev, CI, type-check).
 *   2. The module's public API is pinned in one place.
 *
 * Do NOT populate this file by hand. Run a publish to generate real
 * content in the deploy output.
 */

import type { ChunkRow } from "./types";

export const EMBEDDINGS: ChunkRow[] = [];
export const OWNER_NAME: string = "";
export const PORTFOLIO_ID: string = "";
export const GREETING: string | null = null;
export const STARTERS: string[] = [];
export const GENERATED_AT: string = "";
