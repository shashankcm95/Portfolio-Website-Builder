/// <reference types="@testing-library/jest-dom" />
// Makes jest-dom matchers (`toBeInTheDocument`, `toHaveAttribute`, etc.)
// visible to TypeScript in test files. Runtime registration happens in
// tests/setup/unit.setup.js via `require("@testing-library/jest-dom")`.
